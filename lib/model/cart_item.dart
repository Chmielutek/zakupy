class CartItem {
  final int cartItemId;
  final String productName;
  final int quantity;
  final int quantityPurchased;
// TODO check if any other specs are needed

  CartItem(
      this.cartItemId, this.productName, this.quantity, this.quantityPurchased);
}
