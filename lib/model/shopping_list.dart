class ShoppingList {
  final int id;
  final String name;
  final DateTime creationDate;
  final int itemsCount;
  final int completionPercentage;

  ShoppingList(this.id, this.name, this.creationDate, this.itemsCount,
      this.completionPercentage);
}
